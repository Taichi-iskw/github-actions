name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        description: A cheerful word
        required: true
jobs:
  run:
    runs-on: ubuntu-latest
    env:
      USERNAME: ${{ vars.USERNAME }}
      SAMPLE: ${{ vars.SAMPLE }}
      OBJ: ${{ vars.JSON_SAMPLE }}
      JSON: ${{ vars.OBJ_SAMPLE }}
    steps:
      - run: echo "${OBJ}"
      - run: echo "${JSON}"
