name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        description: A cheerful word
        required: true
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
      - run: echo "hello"
        if: ${{failure()}}
